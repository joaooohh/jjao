const umnumero = 2; 
const segudonumero = 3; 

const soma = umnumero + segudonumero; 
console.log(soma);

const aluno = "fantama";

const soma2 = `O reultado de ${umnumero}${aluno}`;
console.log(soma);
